/* A Good String is a string that string with letter > 3 and should starts with small a */

let string = "puneeth";

if (string[0] === "a" && string.length > 3) {
  console.log("ITS A GOOD STRING");
} else {
  console.log("NOT A GOOD STRING");
}
